
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function isInstitutionalEmail() {
      return request.auth.token.email.toLocaleLowerCase().endsWith('@neu.edu.ph');
    }
    
    // User profiles
    match /users/{userId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && request.auth.uid == userId && isInstitutionalEmail();
    }

    // Admin role verification
    match /admins/{userId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && request.auth.uid == userId && isInstitutionalEmail();
    }

    // Real-time session tracking
    match /active_sessions/{sessionId} {
      allow read: if true;
      allow write, delete: if true;
    }
  }
}
